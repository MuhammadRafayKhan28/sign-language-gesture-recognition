# sign-language-gesture-recognition
This project implements an AI-based system that recognizes basic hand gestures representing common sign language words. The system uses a Convolutional Neural Network (CNN) trained on a labeled dataset of hand gesture images to classify gestures into predefined categories.
